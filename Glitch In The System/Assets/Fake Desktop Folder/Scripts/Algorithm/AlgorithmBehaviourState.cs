namespace GlitchInTheSystem.Algorithm
{
    /// <summary>Trust-driven algorithm temperament (separate from legacy phase 0–2 pacing).</summary>
    public enum AlgorithmBehaviourState
    {
        Passive = 0,
        Assertive = 1,
        Aggressive = 2
    }
}
