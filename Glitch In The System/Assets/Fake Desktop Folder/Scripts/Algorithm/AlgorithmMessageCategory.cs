namespace GlitchInTheSystem.Algorithm
{
    /// <summary>Pre-written clinical message pools used by <see cref="AlgorithmManager"/>.</summary>
    public enum AlgorithmMessageCategory
    {
        OnPlayerHesitation,
        OnPlayerResists,
        OnPlayerComplies,
        OnStressHigh
    }
}
